fn main() {
    let a = 10;
    let b = 2;
    let c = a / b;

    // comentario de linea con / y * adentro
    let d = a / b / 2;

    /* comentario de bloque
       que ocupa varias lineas */
    let e = a * b;

    /* con asterisco a * b adentro */
    /* cerrando con dos asteriscos **/
    let f = a % b;
}